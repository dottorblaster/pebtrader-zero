/*
 * PebTrader Zero - watch-side (embeddedjs) entry point.
 *
 * Imports the Poco app shell. The protocol import proves the shared
 * PKJS/watch field definitions resolve here.
 */

import shell from "./app";

export default shell;
